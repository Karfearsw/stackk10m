[build]
  command = "cp -r FrameworkPlanner/node_modules . && npm --prefix FrameworkPlanner run build && npx --prefix . vite build && cp -r dist/index.html dist/assets FrameworkPlanner/dist/ && rm -rf dist dist-server"
  publish = "FrameworkPlanner/dist"
  install = "npm --prefix FrameworkPlanner ci"

[build.environment]
  NODE_VERSION = "20.x"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
